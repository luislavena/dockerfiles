# == defaults

# Redis provides the following defaults:

# listen on all interfaces (0.0.0.0)
# port 6379

# moderate log verbosity (notice), uses stdout

# 16 databases

# default save intervals:
#
# 1 key every 900 seconds
# 10 keys every 300 seconds
# 10000 keys every 1 minute

# == customization

# do not stop the world if background save couldn't be performed
stop-writes-on-bgsave-error no

# work directory
dir /var/lib/redis/

# == extra configuration
